#include <bits/stdc++.h>
#define MAXN 100
#define MAXK 300
using namespace std;

template <int MAXSIZE>
struct Dijkstra {
	int n, m, d[MAXSIZE], p[MAXSIZE];
	// ...
};

int main() {
    Dijkstra<MAXK * MAXN> sd;
    Dijkstra<MAXN> pd;
}